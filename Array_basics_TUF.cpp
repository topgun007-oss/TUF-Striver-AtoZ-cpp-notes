/*Arrays are used to store multiple values in a single variable, instead of declaring separate variables for each value.

To declare an array, define the variable type, specify the name of the array followed by square brackets and specify the number of elements it should store:

string cars[4];*/
/*
#include<bits/stdc++.h>
using namespace std;
 int main()
 {
      int arr[5]; // this will allocate 5 int sized data space 
      cin>> arr[0] >> arr[1] >> arr[2]>> arr[3] >> arr[4] ;
       return 0;


 }

 for big sized array , use for loop to get input and output

 for( int i = 0 ; i<5; i++){
    cin>>arr[i];
 }

 
 2D array 
 declaration: int arr[3][5]
 This will contain a 2d array with 3 rows and 5 columns*/

 /*
 string s ="Topgun";
 you can access each character  same like array 
   
   cout<< s[0]; 
 
 */



